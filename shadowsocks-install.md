``` shell
$ wget --no-check-certificate -O https://raw.githubusercontent.com/wuyuchang/backup/master/shadowsocks-all.sh # download install script
$ chmod +x shadowsocks-all.sh  # add executation permission
$ ./shadowsocks-all.sh 2>&1 | tee shadowsocks-all.log  # run install shell script with print log
```
